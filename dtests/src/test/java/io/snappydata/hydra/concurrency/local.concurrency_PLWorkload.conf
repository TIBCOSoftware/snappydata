hydra.HostPrms-hostNames =
fcn "hydra.TestConfigFcns.pool(\"<host4>\", ${snappyHosts})" ncf
fcn "hydra.TestConfigFcns.pool(\"<host1> <host2> <host3> <host4>\", ${snappyClientPHosts})" ncf
;

hydra.Prms-maxResultWaitSec = 3600;

io.snappydata.hydra.cluster.SnappyPrms-enableFlightRecorder = true;
io.snappydata.hydra.cluster.SnappyPrms-warmUpTimeSec = 300;
//io.snappydata.hydra.cluster.SnappyPrms-enableGCFlags = true;
