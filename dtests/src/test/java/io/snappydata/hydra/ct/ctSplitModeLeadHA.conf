hydra.Prms-testRequirement = "Test to verify functionality with lead HA using ct(wide) schema";
hydra.Prms-testDescription = "This test starts the snappy cluster and spark cluster.
Test creates tables using sql scripts or snappyJob using ct schema and data.
It then executes sql script in parallel along with lead node and server node HA in parallel.";


INCLUDE $JTESTS/io/snappydata/hydra/ct/leadHA.inc;
INCLUDE $JTESTS/io/snappydata/hydra/ct/ctSplitMode.conf;
INCLUDE $JTESTS/io/snappydata/hydra/ct/backUpLeadConfig.inc;