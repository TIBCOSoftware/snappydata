hydra.Prms-testRequirement = "Test to verify Persistent Partitioned Row tables functionality with server HA using north wind schema";
hydra.Prms-testDescription = "
This test starts the snappy cluster and spark cluster.
Test then runs the sql script for creating and loading data in Persistent Partitioned Row tables using northWind schema and data.
it then executes the spark app and sql script in parallel along with server node HA in parallel.
Spark app executes and validate the northWind queries on the tables created and loaded through embedded mode.
sql script only executes the northWind queries on the tables created and loaded through embedded mode.";

INCLUDE $JTESTS/io/snappydata/hydra/northWind/serverHA.inc;
INCLUDE $JTESTS/io/snappydata/hydra/northWind/nwPersistentPartitionedRowTablesTest.conf;
INCLUDE $JTESTS/io/snappydata/hydra/northWind/backUpServerConfig.inc;