# Do not change the way of queries, sparkProperties, sparkSqlProperties. Just change the values inside strings

#queries to be run
queries="q1,q2,q3,q4,q5,q6,q7,q8,q9,q10"

#spark Properties spefied in lead
sparkProperties="--driver-memory 2g --conf spark.executor.memory=2g"
#sparkProperties="--driver-memory 2g --conf spark.executor.memory=10g --conf spark.network.timeout=300s --conf spark.driver.maxResultSize=2g --conf spark.shuffle.sort.bypassMergeThreshold=3 --conf spark.sql.shuffle.partitions=4 --conf spark.sql.inMemoryColumnarStorage.compressed=false"

sparkSqlProperties="\"\""

#location of checkout
SnappyData=/home/sbhokare/Snappy/repo/snappydata/build-artifacts/scala-2.11/snappy

#Machine Setup
master=pnq-sbhokare
slaves=(pnq-sbhokare)
client=pnq-sbhokare

# location of jar which has related class files
appJar=/home/sbhokare/Snappy/repo/snappydata/cluster/build-artifacts/scala-2.11/libs/snappydata-cluster_2.11-1.0.0-tests.jar

#Size of the TPCH data. Do not chage format
#dataSize=1GB

#Location of the TPCH Data. Make sure directory name is same as the dataSize specified above
dataDir=/home/sbhokare/Snappy/repo/snappydata/data

#QueryPath
queryPath=/home/sbhokare/Snappy/repo/snappydata/spark/sql/core/src/test/resources/tpcds


